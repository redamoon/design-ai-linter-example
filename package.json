{
  "name": "design-ai-linter-example",
  "version": "1.0.0",
  "description": "design-ai-linterを使用したデザインチェックのサンプルリポジトリ",
  "type": "module",
  "scripts": {
    "lint": "dslint lint",
    "fix": "dslint fix",
    "lint:staged": "dslint lint --staged",
    "lint:all": "dslint lint --files \"src/**/*.{tsx,css,ts}\"",
    "postinstall": "node -e \"if (process.env.VOLTA_SKIP_PNPM_SHIM !== '1' && process.env.npm_execpath && process.env.npm_execpath.includes('volta')) { console.log('⚠️  Volta detected. If pnpm fails, use: npm install or VOLTA_SKIP_PNPM_SHIM=1 pnpm install'); }\""
  },
  "keywords": [
    "design-system",
    "linter",
    "figma",
    "tokens"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@design-tools/design-ai-linter": "^0.0.7"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "tsx": "^4.0.0",
    "zod": "^3.22.0"
  },
  "packageManager": "pnpm@8.0.0",
  "volta": {
    "node": "20.18.1",
    "pnpm": "8.0.0"
  }
}

